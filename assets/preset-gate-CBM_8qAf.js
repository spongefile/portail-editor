import{c as n}from"./index-BE18sDw3.js";const a="portail-editor:preset-indexing-ok";function i(e){const r=e.getState(),t=r.status==="connected"?r.firmware??"unknown":"unknown";return`${a}:${t}`}function s(e){try{return localStorage.getItem(i(e))==="1"}catch{return!1}}function c(e){try{localStorage.setItem(i(e),"1")}catch{}}async function u(e){if(s(e))return!0;if(!await n({title:"Confirm your module’s preset numbering (one time)",body:"Preset numbering was recovered by reading the firmware but has not been confirmed on real hardware. Before the first preset save, the editor checks it directly: it will read the current preset, recall it, and read again to make sure the module lands where expected. Nothing is overwritten by this check.",confirmLabel:"Run the check"}))return!1;try{const t=await e.getCurrentPreset();if(t===null)return await n({title:"Could not read the current preset",body:"The module did not report its active preset, so the numbering check cannot run. Preset saving stays locked until this succeeds. Try again with the module connected.",confirmLabel:"OK"}),!1;await e.loadPreset(t);const o=await e.getCurrentPreset();return o===t?(c(e),!0):(await n({title:"Preset numbering did not match",body:`The module reported preset ${t} before and ${o??"nothing"} after recalling it. That is not what this editor expects, so preset saving stays locked to avoid writing the wrong slot. Please report this with your firmware version.`,confirmLabel:"OK"}),!1)}catch{return!1}}export{u as ensurePresetWriteAllowed};
